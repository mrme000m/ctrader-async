[pytest]
markers =
    integration: marks tests as integration tests (connect to real server)
    slow: marks tests as slow
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
